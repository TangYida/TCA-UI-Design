# 重新设计总纲

## 目标

保留 The China Academy 的深金品牌色、原有标志和内容分类，同时把页面从密集信息流改为可持续阅读、可理解层级和可复用组件的编辑型网站。

## 统一设计语言

- **字体**：Libre Baskerville 用于大标题；EB Garamond 用于正文、导语和引文；界面文字统一使用资产目录中的 Roboto 可变字体。
- **色彩**：纸张色为主背景，深蓝为结构色，深金为品牌与会员提示，暗红只标记栏目和关键状态。
- **版式**：全站内容最大宽度为 `1600px`，宽屏使用杂志式不对称网格；正文控制在 `76ch`；小屏按 `930px`、`600px` 和推荐画廊专用的 `580px` 重排。所有文字行高必须在 `1.2–1.5`，不再设置统一段间距下限。
- **导航**：继承早期“Split Desk”方案，品牌居中，栏目与服务分列两侧，移动端折叠。
- **内容卡片**：图片、栏目、标题、摘要、时长/会员状态顺序固定，避免不同页面各做一套卡片。
- **图片**：全部指向源站上传目录；不再存储编辑照片。标志同样使用源站链接。

## 五套母版

母版由实际样式文件 `css/masters.css` 承载，并通过页面 `<body>` 类继承；它不再只是设计文档中的概念分组。

1. **出版首页（`.master-home`）**：主要推荐区在宽屏是两屏横向画廊，第一屏为封面文章，第二屏为 Selected Reading／Most-read 双栏看板；`580px` 以下 Most-read 移为第三屏，禁止与 Selected Reading 上下堆叠。其后依次为 Trending、Thinkers Forum、Premium、Video。
2. **栏目/列表**：大标题 + 筛选标签 + 一篇主推 + 三列卡片；Video 增加时长和播放入口。
3. **文章/详情（`.master-article`）**：标题、标签、导语、作者信息和主视觉分层；正文及图片共用稳定渲染规则；科技与观点只通过内容、标签和必要字段分化。
4. **服务/账户**：课程列表、HSK、支持、搜索和设置使用任务导向布局，清楚显示选择、状态与下一步。
5. **会员聚合（`.master-member`）**：Premium Member 的会员主张独立置于亮米白区块；Intelligence、Courses、Talks 均继承首页非推荐栏目结构——一篇左文右图封面文章加三张 `16:9` 普通卡片。封面文字组与首页使用同一共享规则。

## 文件职责

- `Homepage/`：首页与 Premium Member。
- `Article Sections/`：Trending、Thinkers Forum 与 Premium Intelligence。
- `Video Sections/`：Video 与 Premium Talks 列表。
- `Videos/`：视频文章、Premium Talk 详情与带播放器的课程详情。
- `About/`：Courses、HSK 以及组织、支持、贡献者和作者页面。

原需求是 18 个编号组，但包含 21 个独立网址；设计验收按 21 个 HTML 页面进行。

## 关键交互

- 首次会话自动打开注册弹窗，关闭后本会话不重复自动出现；桌面与移动端同为无侧栏的蓝底白字/金字紧凑面板，仅保留 `Try Free` 按钮且不产生内部滚动条。
- 注册弹窗中的 “Access exclusive…” 使用 EB Garamond 斜体。
- 桌面端粘性导航分为功能栏和标题栏。功能栏向下滚动时折叠；搜索悬浮/聚焦后展开；演示版点击 SIGN IN 即切换为账户图标。
- 标题栏的 Video 与 Premium 有下拉菜单。移动端把两个桌面栏完整合并到一个带高度和透明度过渡的下拉菜单；Home、Trending、Thinkers Forum、Video、Premium、HSK 及功能入口的一级菜单行统一为 `48px`，其父容器不再添加上下内边距，Video/Premium 二级内容采用两行横向滑动。
- 功能栏在 `scrollY > 120` 时完整收起，仅在 `scrollY < 48` 时恢复；缓冲区避免栏高变化反复触发显示状态。
- 视频页面提供明确播放按钮；Premium 内容显示访问状态。
- 所有表单与按钮目前只表达 UI 状态，真实提交需在开发阶段接入。

## 首页内容与阅读时长

- 主封面固定为左图右标题：右侧亮米白方形组件只放主题标签和标题，标签贴顶、标题贴底，标题动态缩放且不超过 `80px`；其 lede 放在图片与标题组件正下方，桌面固定为 `30px`。Trending、Thinkers Forum、Premium、Video 以及 Premium Member 三个栏目的封面固定为左文右图；主题标签、标题和默认 `20px` lede 组成连续文字组，相对右侧图片整体垂直居中，标题与 lede 之间留一倍 lede 行距。发生溢出时才缩小 lede 与标题。
- 悬浮导语只用于主封面和栏目封面。覆层完整覆盖图片，为模糊半透明深蓝底、金色文字，从左向右展开，不使用竖条底纹。
- 栏目标题为左对齐的金色斜体链接，不显示序号、介绍句或 View Section 按钮；悬浮时渐变切换为 `More >>`。
- 旧的圆形箭头导航和其说明文字已经删除。共享 `.lede-row` 把不可点击的 `xx min read` 圆角矩形内联在可点击导语的行末；方框高度为一字高，内部文字为 lede 字号的 `50%`。鼠标悬浮在整个 lede（包括方框）时，时长标签交换前景色与背景色。
- 阅读入口只允许放在标题和 lede；所有封面图和普通文章图均为不可点击的展示容器。标题悬浮显示下划线。
- 主题标签是标题上方的红色 taxonomy 链接，悬浮时原文字渐隐并切换为 `MORE >>`；日期使用独立的灰色 Roboto 元数据样式。Selected Reading 与 Most-read 的栏头不是主题标签。
- 桌面端由 `site.js` 只做防溢出字体适配：主推荐标题从 `80px` 向下缩放；栏目封面先保持增大后的标题基准和 `20px` lede，当方形文字列无法保留一行标题行距时先缩小 lede，再缩小标题。禁止通过定位或负边距让元素相互覆盖。
- 阅读时长算法：正文英文词数除以 `220 words per minute` 后向上取整，且最低显示 1 分钟，界面统一显示为 `xx min read`。观点和科技文章使用已载入正文的词数；尚未接入完整正文的课程、Premium 和视频原型使用 `data-words` 中的编辑预估值。WordPress 接入后应在保存文章时重新统计正文/文字稿并写入字段或缓存。

## 信息密度

非首页页面继续使用紧凑布局和六个相关内容入口。普通文章图片为 `16:9`，移动端封面同样为 `16:9`。所有页面共用 `1.2–1.5` 行高护栏。当前原型用统一内容池验证布局；正式接入时按页面栏目、标签、作者和会员状态动态返回不同内容，避免重复推荐。

文章正文中的图片不再超出阅读栏：`figure` 与直接包含图片的段落在桌面和移动端均为正文栏的 `100%`，图片使用 `height:auto` 保留原始比例。观点和科技文章的桌面封面图在标题网格顶部基础上下移一个标签行及标签下边距（`48px`），使图片上沿与 `h1` 上沿对齐；单栏断点不增加该偏移。该规则写在 `.master-article` / `.article-body` 共享层，后续文章模板无需逐页复制。

## 画廊与分割线

- 主要推荐画廊和栏目小画廊在发生横向溢出时，由 `site.js` 自动生成底部居中的分页圆点；滚动和点击圆点都会更新当前位置。若内容能够一次全部显示，则不呈现圆点。
- 主要推荐画廊不显示 Main Recommendations 或 Swipe 提示。
- 首页板块内部不使用边框或分割线；横线只出现在 Trending、Thinkers Forum、Premium、Video 等板块标题右侧。
- 首页相邻内容板块保持紧凑间距。除主推荐右侧标签/标题组件使用更亮的米白色外，栏目封面文字区、lede 和推荐看板均不另设异色背景。
- 在 `930px` 以下取消封面标题区的固定方形比例，封面图改为 `16:9`，标题和 lede 在图片下方完整显示。
- Trending、Thinkers Forum、Premium、Video 的普通文章画廊与各自封面文章共用同一内容宽度和左侧起点。
- 栏目封面 `cover-heading` 的 `h2` 不再继承水平内边距，标题直接贴齐封面容器左边；`home-section` 在主页基础 gutter 内再增加同等边距，因此栏目标题、封面标题和普通文章拥有明确统一的页面轴线。
- 主要推荐区上下外边距相等。推荐画廊高度始终以第一张封面文章为基准；Most-read 整栏填满这一高度，标题下方的列表占据剩余空间并内部纵向滚动。推荐看板在 `581px` 以上保持 Selected Reading 与 Most-read 左右并置；`580px` 以下脚本把 Most-read 移入第三张横向画廊页，并把整个推荐栏目的高度固定为第一张封面文章高度加 `12px` 圆点导航高度。圆点绝对定位在封面高度之后。
- 主推荐在 `931px` 以上采用互不重叠的两列硬网格：图片只占左列，主题标签和标题只占右列，lede 另起一行置于两列下方。Trending、Thinkers Forum、Premium、Video 在同一阈值以上使用左文右图的反向母版。
- 栏目普通文章宽度为旧宽度的 `4/5`（桌面上限约 `328px`），每个栏目放置 5 篇演示文章。图片、标签/灰色日期、标题、lede/时长的顺序固定。

## 无障碍与边界

- 所有内容图片均有文字替代；纯品牌标志使用空 `alt`，避免重复朗读品牌名。
- 响应式布局不依赖横向页面滚动；标签栏允许局部横向滚动。
- 原型不宣称已经完成键盘全流程、屏幕阅读器、性能、SEO 或生产安全审计。
