# 重新设计总纲

更新日期：2026-08-27

## 目标

保留 The China Academy 的深金品牌色、原有标志和内容分类，同时把页面从密集信息流改为可持续阅读、可理解层级和可复用组件的编辑型网站。

## 统一设计语言

- **字体**：Libre Baskerville 用于大标题；EB Garamond 用于正文、导语和引文；界面文字统一使用资产目录中的 Roboto 可变字体。
- **色彩**：纸张色为主背景，深蓝为结构色，深金为品牌与会员提示，暗红只标记栏目和关键状态。
- **版式**：全站内容最大宽度为 `1600px`，宽屏使用杂志式不对称网格；正文控制在 `76ch`；小屏按 `930px`、`600px` 和推荐画廊专用的 `820px` 重排。所有文字行高必须在 `1.2–1.5`，不再设置统一段间距下限。
- **导航**：继承早期“Split Desk”方案，品牌居中，栏目与服务分列两侧，移动端折叠。
- **内容卡片**：图片、栏目、标题、摘要、时长/会员状态顺序固定，避免不同页面各做一套卡片。主题 taxonomy 只使用 `China’s Economy & Business`、`China’s Politics`、`U.S.`、`China’s Technology`、`China’s Youth Sentiment`、`China’s Worldview` 六项；旧标签由共享脚本集中归类。
- **图片**：全部指向源站上传目录；不再存储编辑照片。标志同样使用源站链接。

## 五套母版

母版由实际样式文件 `css/masters.css` 承载，并通过页面 `<body>` 类继承；它不再只是设计文档中的概念分组。

1. **出版首页（`.master-home`）**：主要推荐区在宽屏是两屏横向画廊，第一屏为带主题与日期的封面文章，第二屏为 Continue Exploring／Most-read 双栏看板；Continue Exploring 使用 Yin／Fan／Zhang／Chris 四门真实课程的等宽 `16:9` 封面、标题和 lede，不显示日期或时长；Most-read 显示主题和日期，并把可点击时长放在 lede 行末。三类内容都由共享逻辑按正文自动选择主题。`820px` 以下 Most-read 移为第三屏，覆盖 iPad 窄窗并禁止两栏重叠。其后依次为 Trending、Opinion、Premium、Video。
2. **Article／Video Section（`.master-section`、`.master-video-section`）**：Trending、Opinion、Premium Intelligence 共用低上留白的金色斜体标题、`All + 六项主题` taxonomy 条、Latest／Popular 排序、分割线卡片看板和 Load More；Trending 与 Opinion 在 All 的首行后插入 Editor's Picks。Video 复用同一卡片几何与四／三／二／一列断点，但拥有独立的 `All + 11 个视频栏目`、Latest／Popular 排序和 `?channel=` 状态，不显示 Editor's Picks；Video 内容分割线使用全局中性色，仅标题栏分割线为金色。
3. **文章/详情（`.master-article`）**：提供头图、文字、新闻三种真实变体。头图版把标签／日期、标题和作者叠在半透明头图上，lede 位于图下；文字版把标题信息居中；新闻版使用日期／标题居中的轻标题区。三者共用正文图片、Editor、右栏推荐、分享、评论和作者联系组件。
4. **服务/账户**：课程列表、HSK、支持、搜索和设置使用任务导向布局，清楚显示选择、状态与下一步。
5. **会员聚合（`.master-member`、`.master-premium-talks`）**：Premium Member 的会员主张独立置于亮米白区块；Intelligence、Courses 继承首页非推荐栏目结构。Premium Talks 不设栏目条和额外会员 CTA，只使用左侧 `16:9` Premium 封面、右侧主题／标题／lede／观看时长的逐行长卡；嘉宾姓名以 EB Garamond 粗体置于 lede 开头，标签前保留红色播放三角，图片中央不放播放字符。

## 文件职责

- `Homepage/`：首页与 Premium Member。
- `Article Sections/`：Trending、Opinion 与 Premium Intelligence。
- `Video Sections/`：Video 与 Premium Talks 列表。
- `Videos/`：视频文章、Premium Talk 详情与带播放器的课程详情。
- `About/`：Courses、HSK 以及组织、支持、贡献者和作者页面。

原需求是 18 个编号组，但包含 21 个独立网址；设计验收按 21 个 HTML 页面进行。

## 关键交互

- 未注册会话进入首页立即打开无遮罩的贴底窗口。窗口与 `36px` 横幅不再作为两个先后离场／进场的色块：二者共用底部边缘和 `--ink`，脚本测量展开内容高度，容器在 `520ms` 内直接伸展或缩小，窗口内容与横幅内容交叉淡化。宽屏仍为说明＋两个等高入口；`601–860px` 隐藏说明文字并为关闭按钮保留 `42px`。Footer 进入视口时当前状态整体下收，离开后恢复。
- 桌面端粘性导航分为功能栏和标题栏。下滑时功能栏在 `280ms` 内折叠，标题栏同时从 `82px` 缩到 `58px`，品牌图标和文字同步缩小；上滑时一起恢复。搜索使用普通浅色并在悬浮/聚焦后展开，SIGN IN 为金色。
- 标题栏的 Video 与 Premium 有下拉菜单；标题本身分别进入 Video Archive 与 Premium Member，Video 子项统一连接 `video.html?channel=栏目名`。点击页面其他区域关闭当前菜单。移动端把两个桌面栏完整合并到固定全屏菜单层，Video／Premium 的标题链接与展开按钮分离且共用 `48px` 行盒，二级内容采用两行横向滑动；支持背景滚动锁、`Escape` 和键盘焦点循环。
- 抽搐来自两个反馈源：功能栏和标题栏过去使用不同阈值，Header 高度动画又会改变浏览器的滚动锚点，从而被监听器误判为反向滚动。本版让两种视觉变化只依赖一个状态，忽略小于 `3px` 的噪声，向下累计 `30px`／向上累计 `36px`，每次切换锁定 `560ms`；锁定期持续刷新滚动基准，同时设置 `overflow-anchor:none`，使 Header 自身变化不能触发反向状态。
- 移动页面启用 `viewport-fit=cover`；浏览器主题色默认白色，打开藏青全屏菜单时同步切换为藏青色，使 iPhone 刘海与其下方区域连成同一色面。
- Footer 删除旧宣传句，在品牌名旁复用源站官方图标；桌面 About、Follow Us、More 使用三个等宽列和统一列间距。About 末尾增加 Partners，悬浮、键盘聚焦或点击时平滑展开 Roboto 字体伙伴列表，当前只有 HSK。其余邮箱、平台字体、逐行账号和紧凑行距规则保持不变；平板两栏、移动端单栏。
- 视频列表不在封面中央放播放按钮；视频身份由标签前红色三角与 `xx min watch` 表达，实际详情页继续提供播放器。Premium 内容的封面左上角显示金底白字访问状态。
- 所有表单与按钮目前只表达 UI 状态，真实提交需在开发阶段接入。

## 首页内容与阅读时长

- 主封面固定为左图右标题：右侧亮米白方形组件只放主题标签和标题，标签贴顶、标题贴底，标题动态缩放且不超过 `80px`；其 lede 放在图片与标题组件正下方，桌面固定为 `30px`。Trending、Opinion、Premium、Video 以及 Premium Member 三个栏目的封面固定为左文右图；主题标签、标题和默认 `20px` lede 组成连续文字组，相对右侧图片整体垂直居中，标题与 lede 之间留一倍 lede 行距。发生溢出时才缩小 lede 与标题。封面标题使用自然左对齐，不做两端拉伸。
- 悬浮导语只用于主封面和栏目封面。覆层完整覆盖图片，为模糊半透明深蓝底、金色文字，从左向右展开，不使用竖条底纹。
- 栏目标题为左对齐的金色斜体链接，不显示序号、介绍句或 View Section 按钮；悬浮时渐变切换为 `More >>`。
- 旧的圆形箭头导航和其说明文字已经删除。共享 `.lede-row` 把可点击的 `xx min read/watch` 圆角矩形内联在可点击导语的行末；方框与导语指向同一内容页，高度为一字高，内部文字为 lede 字号的 `50%`。鼠标悬浮在整个 lede（包括方框）时，脚本读取标签当前文字色并把同一颜色用于背景填充，文字切为纸色，因此不同上下文不再硬编码同一种填充色。
- 主推荐画廊的播放／暂停控制器不使用字符字形或圆形背景；按钮行盒固定为 `10px × 10px`，由 `::before` 以绝对定位绘制 CSS 双竖线或三角形，因此与相邻导航圆点按几何中心对齐，控制器四周 margin 固定为 `0`。
- 阅读入口只允许放在标题和 lede；所有封面图和普通文章图均为不可点击的展示容器。标题悬浮显示下划线。
- 主题标签是标题上方的红色 taxonomy 链接，悬浮时原文字渐隐并切换为 `MORE >>`；日期使用独立的灰色 Roboto 元数据样式。Continue Exploring 与 Most-read 的栏头不是主题标签。
- 桌面端由 `site.js` 只做防溢出字体适配：主推荐标题从 `80px` 向下缩放；栏目封面先保持增大后的标题基准和 `20px` lede，当方形文字列无法保留一行标题行距时先缩小 lede，再缩小标题。禁止通过定位或负边距让元素相互覆盖。
- 阅读时长算法：正文英文词数除以 `220 words per minute` 后向上取整，且最低显示 1 分钟，界面统一显示为 `xx min read`。观点和科技文章使用已载入正文的词数；尚未接入完整正文的课程、Premium 和视频原型使用 `data-words` 中的编辑预估值。WordPress 接入后应在保存文章时重新统计正文/文字稿并写入字段或缓存。
- 视频是独立内容类型：封面媒体保持原宽度但使用 `16:9`，不出现封面引文；首页图片中央不显示播放按钮，视频内容的红色主题标签前统一增加留有间距的小型红色播放三角，并继续使用显式 `data-duration`／`xx min watch`。独立视频页面仍保留实际播放器入口。

## 文章阅读与社区层

- 文章桌面／移动断点为 `900px`。桌面正文使用左目录、中正文、右推荐三栏；左目录的当前章节为红色，右栏分 Continue Exploring 与 Related Reading，每篇推荐附一行小导语，两侧超出视口时独立滚动。
- 左目录首项统一显示当前文章完整标题并链接 `#article-top`；所有正文图片保持原始彩色。主题标签统一继承首页无框红字与 `MORE >>` 动效；作者统一继承头图版的头像蒙版和姓名下划线，文字版仅让整张作者卡居中，卡内姓名与简介左对齐。
- 正文段落与正文图片使用 `1.15em` 垂直节奏；所有文内图片统一与正文等宽并保持 `height:auto`。正式大章节 `h2` 使用粗体和 `28.5ch` 最大文本宽度，编号小节 `h3` 使用常规字重。新闻版每节末项 margin 归零，使章末至下分割线与分割线至下章标题的距离同为 `1.15em`。
- 桌面只显示正文右栏这一组推荐，评论后不再追加 Related Reading 或密度内容流。移动端隐藏普通目录，并把同一个推荐组件移动到评论区之后；新闻版左侧简报入口改为横向滚动。
- 正文结束处只显示右对齐的一行灰色 Editor；随后直接放置无分割线的 X、Facebook、LinkedIn、Reddit、Email 圆形分享图标。Comments 标题采用较小层级，保留 Latest／Popular、顶、踩和回复。
- 跨平台讨论 Version B 位于正文与本站 Comments 之间；两区共用 EB Garamond 标题、muted Roboto 说明、圆形身份标识、身份左／状态右的元数据行及水平 divider。精选流上方设置与 Add a comment 同构的跨平台启动行：LinkedIn／Reddit／Facebook 使用各自主题色底和纸色阴纹主体，YouTube 评论图标使用 SVG 播放符号。外部评论只引用编辑精选内容并把 engagement metadata 作为外链；移动端状态仍留在右列并允许自然折行。
- 头图版由脚本根据图片固有宽度、正文栏宽和页面可用宽度计算最终宽度；藏青半透明蒙版始终覆盖图片，红色无边框主题标签保留全局 `MORE >>` 动效，标题和作者随容器缩放以避免溢出。
- 作者悬浮只给头像增加半透明藏青蒙版并给名字加下划线，不把照片变为灰度或单色；激活后打开藏青 Contact the Author 弹窗，包含关闭 `×`、作者简介、Learn more、Email 和 Message。

## 信息密度

非首页、非文章页面继续使用紧凑布局和六个相关内容入口；文章页改用自己的单一推荐组件，避免评论前后重复。普通文章图片为 `16:9`，所有页面共用 `1.2–1.5` 行高护栏。正式接入时按栏目、标签、作者和会员状态动态返回不同内容。

文章正文中的图片统一拉满阅读栏：承载图片的 `figure` 或段落占正文栏 `100%`，图片本身使用 `width:100%; max-width:100%; height:auto`。观点和科技文章的桌面封面图继续与 `h1` 上沿对齐；该规则只写在 `.master-article` / `.article-body` 共享层，后续文章模板无需逐页复制。

## 画廊与分割线

- 主要推荐画廊和栏目小画廊在发生横向溢出时，由 `site.js` 自动生成底部居中的分页圆点；滚动和点击圆点都会更新当前位置。若内容能够一次全部显示，则不呈现圆点。
- 主要推荐圆点附带 10 秒自动播放计时：活动圆点以扇形面积显示进度，圆点前提供播放／暂停按钮。`820px` 以下控制区与主推荐 lede 间隔一倍 lede 行距，并计入推荐区总高度。
- 主要推荐画廊不显示 Main Recommendations 或 Swipe 提示。
- 首页板块内部不使用边框或分割线；横线只出现在 Trending、Opinion、Premium、Video 等板块标题右侧。
- 首页相邻内容板块保持紧凑间距。除主推荐右侧标签/标题组件使用更亮的米白色外，栏目封面文字区、lede 和推荐看板均不另设异色背景。
- 在 `930px` 以下取消封面标题区的固定方形比例，封面图改为 `16:9`，标题和 lede 在图片下方完整显示。
- Trending、Opinion、Premium、Video 的普通文章画廊与各自封面文章共用同一内容宽度和左侧起点。
- 栏目封面 `cover-heading` 的 `h2` 不再继承水平内边距，标题直接贴齐封面容器左边；`home-section` 在主页基础 gutter 内再增加同等边距，因此栏目标题、封面标题和普通文章拥有明确统一的页面轴线。
- 主要推荐区上下外边距相等。推荐画廊高度始终以第一张封面文章为基准；Most-read 整栏填满这一高度，标题下方的列表占据剩余空间并内部纵向滚动。推荐看板在 `821px` 以上保持 Continue Exploring 与 Most-read 左右并置；`820px` 以下脚本把 Most-read 移入第三张横向画廊页。四张 Continue Exploring 卡片共用 `minmax(0,1fr)` 等宽列、相同 `16:9` 媒体框、两行标题和一行 lede 上限，避免手机和 `778px` 窄平板下出现尺寸漂移。
- 主推荐在 `931px` 以上采用互不重叠的两列硬网格：图片只占左列，主题标签和标题只占右列，lede 另起一行置于两列下方。Trending、Opinion、Premium、Video 在同一阈值以上使用左文右图的反向母版。
- 栏目普通文章宽度为旧宽度的 `4/5`（桌面上限约 `328px`），每个栏目放置 5 篇演示文章。图片、标签/灰色日期、标题、lede/时长的顺序固定。
- `Beta Demo/homepage.html` 是隔离实验：桌面封面模块统一改为 `4:3`，普通文章画廊采用两行横向网格。该规则只有 URL 参数 `demo=beta-4x3` 激活，尚未合并到正式首页。

## 无障碍与边界

- 所有内容图片均有文字替代；纯品牌标志使用空 `alt`，避免重复朗读品牌名。
- 响应式布局不依赖横向页面滚动；标签栏允许局部横向滚动。
- 原型不宣称已经完成键盘全流程、屏幕阅读器、性能、SEO 或生产安全审计。
