# 开发进度与交接清单

更新日期：2026-08-20

## 一、UI设计备忘

### 维护文件结构和基础设计

- [x] 建立 `README.md`、`REDESIGN.md`、`MAP.md`、`TODO.md` 维护机制。
- [x] 为清单中的 21 个网址建立独立设计文件并写入 `MAP.md`。
- [x] 建立四套可继承母版，并用 Mermaid 记录页面关系。
- [x] 抽离全站 `tokens.css`、`site.css` 和 `site.js`。
- [x] 将 EB Garamond、Libre Baskerville 放入 `assets/fonts/` 并本地加载。
- [x] 将旧导航、衬线与文章排版研究移入 `references/`。
- [x] 将旧照片和旧的超大内嵌图片文章原型移至废纸篓；新页面仅用源站图片 URL。
- [x] 保留注册弹窗文案及首次会话自动出现逻辑。
- [x] 完成桌面、平板和手机的基础响应式规则。
  
### 主页和文章（不包括视频等）

- [x] 将母版落实为实际存在的 `css/masters.css`；全局文章图片模式由 `.master-article .wp-source-content` 统一控制，并在 `MAP.md` 写明维护边界。
- [x] 主页回滚到已确认的封面文章、各栏目封面文章、横向画廊和图片悬浮导语设计。
- [x] 观点文章与科技文章重新锚定源站，静态载入完整正文、作者信息、正文图片/图注和相关推荐。
- [x] 保留文章标签式说明文字；功能性标题不使用该样式，注册提示框已移除 `Stay updated` 眉题。
- [x] 注册提示框统一桌面/移动端：无左侧栏、无内部滚动、紧凑蓝底白字/金字，并用 `Try Free` 按钮替代邮箱输入框。

### 全站导航、信息密度与主页（合并后的完成项）

- [x] 注册提示框保持桌面/移动端一致，并将 “Access exclusive…” 改为 EB Garamond 斜体。
- [x] 桌面导航拆分为功能栏和标题栏；功能栏仅保留 Support Us、About Us、搜索和演示登录，使用带缓冲阈值的完整收起逻辑修复半显/半隐抖动。
- [x] 标题栏改为 Home、Trending、Thinkers Forum／品牌／Video、Premium、HSK，移除 `CHINA IN FULL`；Video 与 Premium 提供下拉内容。
- [x] 移动端将两层桌面导航合并为带动效的菜单；Video/Premium 二级菜单以两行横向滑动方式展示。
- [x] 演示版 SIGN IN 点击后切换为账户图标，再次点击进入个人中心；正式接入仍需替换为 WordPress 登录状态。
- [x] 主页重构为“主要推荐横向大画廊 + Trending + Thinkers Forum + Premium + Video”；主画廊右滑后显示普通文章与 Most-read of the Month 双栏看板。
- [x] 推荐看板接入源站的政治制度课程和退役 EV 电池文章及图片；Most-read 竖栏使用五篇演示文章验证滚动密度。
- [x] 桌面端封面文章图片统一为 `4:3`；主推荐采用左图右文，栏目封面采用左文右图，标题、导语和导航统一放在文字列；普通文章图片统一为 `16:9`。
- [x] 移动端封面图保持 `16:9`，标题与导语完整显示；字号和间距回滚到原始响应式版本，不再统一缩减 50%。
- [x] 正文、导语、摘要和卡片说明回滚到原始字号与间距；桌面恢复各组件原始行距，移动端正文统一使用 `1.5` 倍行距。
- [x] 主封面保持左图右文，栏目封面保持右图左文；封面悬浮导语仅用于封面文章，蓝色模糊覆层完整覆盖图片，文字自适应缩放并完整居中显示。
- [x] Trending 等栏目小标题改为金色；悬浮时渐变切换为 `More >>`。
- [x] 首页移除主封面下方的 History／Political Economy 元信息；导语和圆形导航组位于同一行，导航组靠右；水平箭头保留缩放/变色但不旋转，阅读时长位于按钮正下方并改用非斜体无衬线。
- [x] 日期放在标题上方标签之后；画廊标签与图片保留一行间距，移除画廊卡片顶部横线和非封面图片的导语动画。
- [x] 观点、科技、视频和 Premium Talk 的标题标签改为可点击链接，并由搜索页读取 `tag` 参数显示对应标签结果标题。
- [x] 大画廊和小画廊按实际溢出自动生成底部居中的分页圆点并跟随滚动更新；能一次显示全部内容时不生成圆点，同时移除 Main Recommendations 和 Swipe 提示。
- [x] 移除首页各板块内部的边框和分割线；只在 Trending、Thinkers Forum、Premium、Video 等板块标题右侧保留一条横线。
- [x] 修正平板断点（包括 `949×849`）的封面标题留白：取消标题容器固定高度；主推荐导语直接进入 `cover-heading` 并紧跟标题；推荐画廊圆点与内容之间维持紧凑间距。
- [x] 首页相邻板块的上下间距缩小一半；清除封面标题、导语和推荐看板的异色背景，并明确让栏目普通文章与封面文章共用同一左边界。
- [x] 二次校正栏目对齐：栏目封面 `h2` 取消 `cover-heading` 水平内缩，直接贴齐封面容器左端；`home-section` 在主页既有 gutter 内再增加一倍页面边距，普通文章与封面标题使用相同起点。
- [x] 主要推荐区上下边距改为相等并进一步缩小；将主推荐 `cover-footer` 移入 `cover-heading`，不再作为图片和标题下方的独立整行；同时压缩推荐看板内边距和 Most-read 竖栏高度。iPad 保留双栏，手机才转为单栏。
- [x] 修复主推荐图片覆盖文字：在 `760px` 以上明确使用左右双栏，图片固定在左列，`cover-heading` 固定在右列并以标题在上、导语/导航在下的顺序排布；窄屏才恢复上下堆叠。
- [x] 四个栏目封面的 `cover-footer` 已移入各自 `cover-heading`；母版保留两种明确方向：主推荐图片左、文字右，栏目封面文字左、图片右。两种方向都使用硬网格边界，并取消标题列宽高比和旧 `order` 的实际控制权。
- [x] 将 Trending、Thinkers Forum、Premium、Video 栏目封面的导航组向左收进 `18–30px`，避免圆形按钮紧贴右侧封面图。
- [x] 调整文章共享母版：正文中的 `figure` 和直接图片段落在桌面/移动端均统一为正文栏的 `100%`，图片保持 `height:auto` 和原始宽高比；桌面文章封面图下移标签行及其间距的 `48px`，使图片上沿与 `h1` 上沿对齐。
- [ ] 封面文章的图片：在桌面端的时候固定1:1；如果屏幕宽度变小，图片左边的文字内容也要跟着变小；一旦封面文章图片的宽度小于普通文章图片的宽度，就达到阈值，进入移动端模式
- [ ] 封面文章的标题：在移动端的时候也应当与图片保持左对齐，右侧与导航按钮右侧保持对齐——这样就不会离图片太近


## 二、下一阶段的交接清单

- [ ] 将静态页面映射为 WordPress/PHP 模板或现有前端框架组件。
- [ ] 用 CMS 数据替换演示文案、硬编码日期、作者、卡片和图片 URL。
- [ ] 将原型中的统一“Continue exploring”内容池替换为基于栏目、标签、作者、会员状态的 WordPress 查询，避免不同页面出现相同推荐。
- [ ] 在 WordPress 保存文章或文字稿时自动统计词数并缓存阅读时长，替换课程、Premium 和视频原型中的 `data-words` 编辑预估值。

### 建议

- [ ] 完善站内搜索（尤其是作者），视频页面和油管主页内容保持动态更新
- [ ] Contributor Profile 与 Author Archive 是否继续作为两个独立内容模型。
- [ ] 上传封面的最低尺寸、宽高比、文件大小与自动裁切规则；建议先以主封面 `1600×1100` 作为起点
- [ ] 网站性能优化：字体子集化、srcset、LCP等等、优化CDN速度
