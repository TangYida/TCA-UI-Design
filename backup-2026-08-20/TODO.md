# 开发进度与交接清单

更新日期：2026-08-20

## 一、UI设计备忘

### 维护文件结构和基础设计

- [x] 建立 `README.md`、`REDESIGN.md`、`MAP.md`、`TODO.md` 维护机制。
- [x] 为清单中的 21 个网址建立独立设计文件并写入 `MAP.md`。
- [x] 建立四套可继承母版，并用 Mermaid 记录页面关系。
- [x] 抽离全站 `tokens.css`、`site.css` 和 `site.js`。
- [x] 将 EB Garamond、Libre Baskerville 放入 `assets/fonts/` 并本地加载。
- [x] 将旧导航、衬线与文章排版研究移入 `references/`。
- [x] 将旧照片和旧的超大内嵌图片文章原型移至废纸篓；新页面仅用源站图片 URL。
- [x] 保留注册弹窗文案及首次会话自动出现逻辑。
- [x] 完成桌面、平板和手机的基础响应式规则。

### 主页和文章（不包括视频等）

- [x] 将母版落实为实际存在的 `css/masters.css`；全局文章图片模式由 `.master-article .wp-source-content` 统一控制，并在 `MAP.md` 写明维护边界。
- [x] 主页回滚到已确认的封面文章、各栏目封面文章、横向画廊和图片悬浮导语设计。
- [x] 观点文章与科技文章重新锚定源站，静态载入完整正文、作者信息、正文图片/图注和相关推荐。
- [x] 保留文章标签式说明文字；功能性标题不使用该样式，注册提示框已移除 `Stay updated` 眉题。
- [x] 注册提示框统一桌面/移动端：无左侧栏、无内部滚动、紧凑蓝底白字/金字，并用 `Try Free` 按钮替代邮箱输入框。

### 全站导航、密度与第二轮主页细节

- [x] 注册提示框的 “Access exclusive…” 改用斜体。
- [x] 所有非首页页面压缩约一半留白和卡片间距，并新增六条相关内容入口；移动端封面/卡片图片统一为 `16:9`，标题和导语保持完整显示。
- [x] 桌面导航拆分为功能栏和标题栏；功能栏仅保留 Support Us、About Us、搜索和演示登录，并在向下滚动后隐藏。
- [x] 标题栏改为 Home、Trending、Thinkers Forum／品牌／Video、Premium、HSK，移除 `CHINA IN FULL`；完成 Video 与 Premium 下拉内容。
- [x] 移动端将两层桌面导航完整并入带动效的下拉菜单，并支持 Video/Premium 二级展开。
- [x] 演示版 SIGN IN 点击后切换为账户图标，再次点击进入个人中心；正式接入仍需替换为 WordPress 登录状态。
- [x] 首页主封面调整为左图右文，所有栏目封面统一为右图左文；悬浮导语仅保留在封面文章，并改成半透明蓝底金字、从左向右展开、无竖条纹。
- [x] 在 Trending 与 Thinkers Forum 后增加 Premium 板块。
- [x] 首页引导按钮改为圆形箭头；阅读时长按 `ceil(正文词数 ÷ 220)` 计算并置于右下角。
- [x] 首页日期移至标题上方的红色标签之后；栏目标题移除序号、说明和 View Section，改为蓝色斜体链接及悬浮下划线/放大。
- [x] 画廊标签与图片增加一行间距，移除画廊卡片顶部横线，并取消非封面图片的悬浮导语。

### 第三轮反馈修改

- [x] 首页板块小标题（Trending / Thinkers Forum / Premium / Video）改为金色，悬浮时渐变切换为 `More >>`。
- [x] 封面箭头按钮去除旋转动效（保留缩放、背景与颜色过渡）；阅读时长移至圆形按钮正下方；移除封面文章的 `History / Political Economy` meta。
- [x] 修复封面文章悬浮导语蓝色覆层未完全覆盖封面图片的问题（`.story-cover` 图片绝对铺满容器，覆层 `inset` 改为 `0`）。
- [x] 移动端 Video / Premium 二级下拉菜单改为两列显示；修复功能栏出现/消失一半时画面抽搐（滚动监听加入 4px 死区与方向判定）。
- [x] 所有文章页标签（标题上方说明文字，含 `.article-tag` 与视频/会员页 `.eyebrow`）改为可点击，跳转标签搜索页 `search.html?tag=…`。
- [x] 重构主页结构：主要推荐板块改为大画廊（封面文章向右滑动露出看板），看板两列为 `Editor’s picks` 普通文章 + `Most-read of the Month` 竖向滑动栏；保留 Trending、Thinkers Forum、Premium、Video 四个板块。

## 二、下一阶段的交接清单

- [ ] 将静态页面映射为 WordPress/PHP 模板或现有前端框架组件。
- [ ] 用 CMS 数据替换演示文案、硬编码日期、作者、卡片和图片 URL。
- [ ] 将原型中的统一“Continue exploring”内容池替换为基于栏目、标签、作者、会员状态的 WordPress 查询，避免不同页面出现相同推荐。
- [ ] 在 WordPress 保存文章或文字稿时自动统计词数并缓存阅读时长，替换课程、Premium 和视频原型中的 `data-words` 编辑预估值。

### 建议

- [ ] 完善站内搜索（尤其是作者与标签）；标签搜索页 `search.html?tag=` 目前为演示文案，需接入真实标签/分类查询；视频页面和油管主页内容保持动态更新。
- [ ] Contributor Profile 与 Author Archive 是否继续作为两个独立内容模型。
- [ ] 上传封面图需提供 `16:9` 与 `1:1` 两种比例的参考线，便于编辑确认关键内容在两种裁切下都完整；并明确最低尺寸、文件大小与自动裁切规则（建议先以主封面 `1600×1100` 作为起点）。
- [ ] 网站性能优化：字体子集化、srcset、LCP等等、优化CDN速度。
